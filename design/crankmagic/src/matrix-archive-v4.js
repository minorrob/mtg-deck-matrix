let archivedDecks=[],showArchived=false;
function archiveDeck(deck){const list=rows.filter(r=>r.deck===deck),owned=list.filter(r=>r.source==='Owned').length,ordered=list.filter(r=>r.source==='Ordered').length;confirmChange('Archive '+deck,`Remove ${deck} from build and allocation choices. This preview will release its ${owned} tracked owned copies and ${ordered} tracked orders; owned copies fulfill compatible unmet needs or go to the bench. Physical box locations remain recorded as move tasks. The saved list is retained; new purchase needs for this deck are removed.`,()=>transact(deck+': archived; copies and orders retained, allocations released.',()=>{archivedDecks.push(deck);candidate=null;q('#v-swap-panel').hidden=true;for(const r of list){const from=r.location;const need=r.source==='Owned'?rows.find(x=>x!==r&&x.deck!==deck&&x.deck&&!archivedDecks.includes(x.deck)&&x.name===r.name&&x.source==='To buy'):null;if(need){r.deck=need.deck;r.slot=need.slot;r.purpose=need.purpose;r.placement='Reserved';r.location=from+' / move pending';rows=rows.filter(x=>x!==need);}else if(r.source==='To buy')rows=rows.filter(x=>x!==r);else{r.deck='';delete r.slot;r.purpose='Unassigned';r.placement=r.source==='Owned'?(r.placement==='In deck'?'Move pending':'Bench'):'Unassigned';r.location=from+(r.source==='Owned'&&r.placement==='Move pending'?' / move pending':'');}}}));}
function renderArchiveUI(){qa('[data-deck-tile]').forEach(el=>el.hidden=archivedDecks.includes(el.dataset.deckTile));q('#v-deck-count').textContent=String(3-archivedDecks.filter(d=>['Atraxa','Krenko','Shadrix'].includes(d)).length).padStart(2,'0');q('#v-deck-workspace').hidden=archivedDecks.includes('Atraxa');q('#v-archive-undo').disabled=!undo.length;q('#v-archived-list').hidden=!showArchived;q('#v-archived-list').innerHTML=archivedDecks.length?archivedDecks.map(deck=>`<div class="v-archived-deck"><strong>${esc(deck)}</strong><span class="v-small">Archived · list retained · allocations released</span><button class="v-button" type="button" data-view-archived="${deck}">View saved overview</button></div>`).join(''):'<p class="v-small">No archived decks.</p>';qa('[data-view-archived]').forEach(b=>b.onclick=()=>{showOverview(b.dataset.viewArchived);q('#v-overview-tune').disabled=true;q('#v-overview-tune').textContent='Archived';q('#v-overview-swaps').disabled=true;q('#v-overview-cards').textContent='View current Collection';q('#v-overview-cards').onclick=()=>{q('#v-show-all-collection').click();view('collection');};});}
qa('[data-archive-deck]').forEach(b=>b.onclick=()=>archiveDeck(b.dataset.archiveDeck));q('#v-show-archived').onclick=()=>{showArchived=!showArchived;q('#v-show-archived').setAttribute('aria-expanded',String(showArchived));renderArchiveUI();};q('#v-archive-undo').onclick=()=>q('#v-undo').click();
