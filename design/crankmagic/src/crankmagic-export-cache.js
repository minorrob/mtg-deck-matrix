// Export-only storage adapter. The sandbox stays intact. Only its own iframe
// may read/write this one preview-report key; no arbitrary storage operations.
(()=>{const frame=document.querySelector('iframe'),key='crankmagic.design.preview-reports.v1';addEventListener('message',e=>{const m=e.data;if(e.source!==frame?.contentWindow||m?.channel!=='crankmagic-preview-cache'||!['read','write'].includes(m.operation)||typeof m.request!=='string')return;let result;try{if(m.operation==='write'){if(!Array.isArray(m.reports)||m.reports.length>10||JSON.stringify(m.reports).length>300000)throw Error('Invalid preview payload');localStorage.setItem(key,JSON.stringify(m.reports));}result={ok:true,reports:m.operation==='read'?JSON.parse(localStorage.getItem(key)||'[]'):undefined};}catch{result={ok:false};}e.source.postMessage({channel:'crankmagic-preview-cache-reply',request:m.request,...result},'*');});})();
